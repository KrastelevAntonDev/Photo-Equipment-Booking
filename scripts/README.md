# 🛠️ Scripts Directory

## 📋 Доступные скрипты

### `init-uploads-volume.sh`
**Назначение:** Инициализация Docker volume с существующими файлами из репозитория

**Когда использовать:**
- При первом развёртывании на новом сервере
- Когда нужно скопировать файлы из `./uploads` в volume

**Использование:**
```bash
chmod +x scripts/init-uploads-volume.sh
./scripts/init-uploads-volume.sh
```

**Что делает:**
1. Проверяет существование volume `apipicassostudioru_uploads_data`
2. Создаёт volume, если его нет
3. Проверяет, пустой ли volume
4. Если пустой - копирует файлы из `./uploads/equipment` и `./uploads/rooms`
5. Показывает статистику

**Безопасность:**
- Не перезаписывает существующие файлы
- Работает только с пустым volume
- Read-only доступ к исходным файлам

---

### `backup-uploads-volume.sh`
**Назначение:** Создание резервной копии volume

**Когда использовать:**
- Перед обновлением кода на production
- Регулярно (настроить в cron для ежедневного выполнения)
- Перед миграцией на другой сервер

**Использование:**
```bash
chmod +x scripts/backup-uploads-volume.sh
./scripts/backup-uploads-volume.sh
```

**Что делает:**
1. Создаёт директорию `/backups/uploads` (если не существует)
2. Создаёт `.tar.gz` архив volume с timestamp в имени
3. Показывает размер созданного архива
4. Удаляет бэкапы старше 30 дней
5. Показывает список всех доступных бэкапов

**Параметры:**
- Директория бэкапов: `/backups/uploads`
- Формат имени: `uploads_YYYYMMDD_HHMMSS.tar.gz`
- Хранение: 30 дней

**Настройка в cron:**
```bash
crontab -e

# Добавить строку (выполнение каждый день в 2:00):
0 2 * * * cd /path/to/project && ./scripts/backup-uploads-volume.sh >> /var/log/uploads-backup.log 2>&1
```

---

### `restore-uploads-volume.sh`
**Назначение:** Восстановление volume из резервной копии

**Когда использовать:**
- После случайного удаления volume
- При повреждении файлов
- При миграции на новый сервер

**Использование:**
```bash
chmod +x scripts/restore-uploads-volume.sh

# Показать список доступных бэкапов
./scripts/restore-uploads-volume.sh

# Восстановить из конкретного бэкапа
./scripts/restore-uploads-volume.sh /backups/uploads/uploads_20240115_020000.tar.gz

# Можно указать только имя файла (без пути)
./scripts/restore-uploads-volume.sh uploads_20240115_020000.tar.gz
```

**Что делает:**
1. Проверяет существование файла бэкапа
2. Спрашивает подтверждение (требуется ввести "yes")
3. Останавливает контейнеры (`docker compose down`)
4. Очищает volume
5. Распаковывает архив в volume
6. Показывает статистику
7. Напоминает запустить контейнеры

**⚠️ ВНИМАНИЕ:**
- Полностью перезаписывает содержимое volume
- Требует явное подтверждение
- Останавливает контейнеры автоматически

---

### `copy-public.js`
**Назначение:** Копирование статических файлов после сборки TypeScript

**Когда запускается:**
- Автоматически после `pnpm build` (postbuild hook)

**Что делает:**
1. Копирует содержимое `src/public` в `dist/public`
2. **Пропускает** директорию `uploads` (чтобы не перезаписывать пользовательские файлы)
3. Создаёт пустую структуру директорий для uploads

**Не требует ручного запуска!**

---

## 🚀 Типичные сценарии использования

### Сценарий 1: Первое развёртывание
```bash
# 1. Дать права на выполнение всем скриптам
chmod +x scripts/*.sh

# 2. Инициализировать volume
./scripts/init-uploads-volume.sh

# 3. Запустить приложение
docker compose up -d --build

# 4. Настроить ежедневный бэкап
crontab -e
# Добавить: 0 2 * * * cd $(pwd) && ./scripts/backup-uploads-volume.sh
```

### Сценарий 2: Обновление на production
```bash
# 1. Создать бэкап перед обновлением
./scripts/backup-uploads-volume.sh

# 2. Обновить код
git pull

# 3. Пересобрать
docker compose down
docker compose build --no-cache
docker compose up -d

# 4. Проверить
curl http://localhost:5000/health
```

### Сценарий 3: Восстановление после сбоя
```bash
# 1. Посмотреть список бэкапов
./scripts/restore-uploads-volume.sh

# 2. Выбрать подходящий и восстановить
./scripts/restore-uploads-volume.sh uploads_20240115_020000.tar.gz

# 3. Запустить
docker compose up -d
```

### Сценарий 4: Миграция на новый сервер
```bash
# На СТАРОМ сервере:
./scripts/backup-uploads-volume.sh
scp /backups/uploads/uploads_*.tar.gz user@new-server:/tmp/

# На НОВОМ сервере:
git clone <repo>
cd <project>
chmod +x scripts/*.sh
docker volume create apipicassostudioru_uploads_data
./scripts/restore-uploads-volume.sh /tmp/uploads_*.tar.gz
docker compose up -d --build
```

---

## 📊 Проверка работы скриптов

### Тест init-uploads-volume.sh
```bash
# Удалить volume (если существует)
docker volume rm apipicassostudioru_uploads_data

# Запустить скрипт
./scripts/init-uploads-volume.sh

# Проверить результат
docker run --rm \
  -v apipicassostudioru_uploads_data:/data \
  alpine ls -lah /data

# Ожидается:
# drwxr-xr-x ... equipment/
# drwxr-xr-x ... rooms/
```

### Тест backup-uploads-volume.sh
```bash
# Создать бэкап
./scripts/backup-uploads-volume.sh

# Проверить, что файл создан
ls -lh /backups/uploads/

# Проверить содержимое архива
tar tzf /backups/uploads/uploads_*.tar.gz | head -20
```

### Тест restore-uploads-volume.sh
```bash
# Создать тестовый volume с данными
docker volume create test_uploads_data
docker run --rm \
  -v test_uploads_data:/data \
  alpine sh -c 'echo "original" > /data/test.txt'

# Восстановить из бэкапа (перезапишет test.txt)
docker volume create apipicassostudioru_uploads_data
# (в скрипте нужно временно изменить VOLUME_NAME на test_uploads_data)
./scripts/restore-uploads-volume.sh /backups/uploads/uploads_LATEST.tar.gz

# Проверить
docker run --rm \
  -v test_uploads_data:/data \
  alpine cat /data/test.txt
# НЕ должно быть "original", должно быть содержимое из бэкапа

# Удалить тестовый volume
docker volume rm test_uploads_data
```

---

## 🔧 Настройка параметров

### Изменить директорию бэкапов
Отредактировать `backup-uploads-volume.sh`:
```bash
BACKUP_DIR="/custom/backup/path"  # Изменить путь
```

### Изменить время хранения бэкапов
Отредактировать `backup-uploads-volume.sh`:
```bash
KEEP_DAYS=60  # Хранить 60 дней вместо 30
```

### Изменить имя volume
Отредактировать все три скрипта:
```bash
VOLUME_NAME="custom_volume_name"  # Вместо apipicassostudioru_uploads_data
```

---

## ⚠️ Требования

- **Docker** установлен и запущен
- **Права на выполнение** скриптов (`chmod +x`)
- **Достаточно места** для бэкапов (минимум 2x размер volume)
- **Bash** 4.0+ (для alpine контейнеров используется sh)

---

## 📝 Логи

### Логи бэкапа (если настроен cron)
```bash
tail -f /var/log/uploads-backup.log
```

### Ручной запуск с логированием
```bash
./scripts/backup-uploads-volume.sh 2>&1 | tee -a /var/log/manual-backup.log
```

---

## 🆘 Troubleshooting

### Проблема: "Permission denied"
```bash
# Решение: Дать права на выполнение
chmod +x scripts/*.sh
```

### Проблема: "Volume not found"
```bash
# Решение: Создать volume
docker volume create apipicassostudioru_uploads_data
```

### Проблема: "No space left on device"
```bash
# Решение: Освободить место или изменить BACKUP_DIR
df -h
docker system prune -a  # Освободить место
```

### Проблема: Бэкап слишком большой
```bash
# Проверить размер volume
docker system df -v | grep uploads_data

# Возможно, нужно почистить старые файлы в volume
docker run --rm \
  -v apipicassostudioru_uploads_data:/data \
  alpine find /data -type f -mtime +365 -ls  # Показать файлы старше года
```

---

## ✅ Best Practices

1. **Регулярные бэкапы** - настроить cron для ежедневного выполнения
2. **Тестировать восстановление** - раз в месяц проверять, что бэкапы рабочие
3. **Хранить off-site** - копировать бэкапы на удалённое хранилище
4. **Мониторинг размера** - следить за ростом volume и бэкапов
5. **Документировать изменения** - записывать когда и зачем восстанавливались бэкапы

---

## 📚 Связанная документация

- [DEEP_AUDIT_UPLOADS.md](../docs/DEEP_AUDIT_UPLOADS.md) - Технический аудит
- [DEPLOYMENT_GUIDE.md](../docs/DEPLOYMENT_GUIDE.md) - Руководство по развёртыванию
- [UPLOADS_QUICK_REF.md](../docs/UPLOADS_QUICK_REF.md) - Краткая справка
